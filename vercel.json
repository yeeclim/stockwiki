{
  "buildCommand": "bash build-flutter.sh",
  "outputDirectory": "build/web",
  "installCommand": "npm install",
  "framework": null,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
